import { PipesResolverPipe } from './pipes-resolver.pipe';

/*
describe('PipesResolverPipe', () => {
  it('create an instance', () => {
    const pipe = new PipesResolverPipe();
    expect(pipe).toBeTruthy();
  });
});
*/
